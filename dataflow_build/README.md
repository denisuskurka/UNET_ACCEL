# Exporting torch model to stitched-IP

To export the torch model best_unet_weights.pth used the unet.ipynb notebook.
This notebook converts the pth to onnx format, applies 

Copy the unet.ipynb to finn repo in finn/notebook/unet
Also copy the model to export and model.py and common.py for model structure.

Run the jupyter notebook in FINN

